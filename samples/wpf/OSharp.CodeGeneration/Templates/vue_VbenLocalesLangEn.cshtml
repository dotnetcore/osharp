@inherits MiniRazor.TemplateBase<CodeProject>
@namespace OSharp.CodeGeneration.Templates
@using System;
@using System.Linq;
@using OSharp.CodeGeneration.Services.Entities
@using OSharp.CodeGeneration.Utils
@using OSharp.Collections;
@using OSharp.Exceptions;
@using OSharp.Extensions;
@{
  CodeProject project = Model;
}// -----------------------------------------------------------------------
//  <once-generated>
//     这个文件只生成一次，再次生成不会被覆盖。
//  </once-generated>
//
//  <copyright file="src/locales/lang/en/routers/osharp.ts" company="@project.Company">
//      @project.Copyright
//  </copyright>
//  <site>@project.SiteUrl</site>
//  <last-editor>@project.Creator</last-editor>
// -----------------------------------------------------------------------
export default {
  system: 'System',
  @foreach (var module in project.Modules)
  {
    string moduleName = module.Name;
    string lowerModule = moduleName.ToLowerCase();
<text>  @lowerModule: {
    @lowerModule: '@moduleName',
    @foreach (var entity in module.Entities)
    {
<text>    @entity.Name.ToLowerCase(): '@entity.Name',
</text>
    }
  },
</text>
  }
};
